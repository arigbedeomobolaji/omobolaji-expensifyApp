// Mocking meams creating a fake version of a module which  defiens what happens when the code actually call the module
// getting guidance from the JEST DOCUMENTATION
// in the JEST doc go to MANUAL MOCKS page === This is where we the test writer have the option to create mock's for various libraries.

// When I call the module I'm trying to mock it will use the function exported from the node_modules whereas the momcked version will us the function exported from the mock file